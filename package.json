{
  "name": "vanshul-links",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "links.vanshul.com — a single-page linktree for Vanshul Goyal (Curious Ape).",
  "homepage": "https://links.vanshul.com",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "sitemap": "node scripts/gen-sitemap.mjs"
  },
  "devDependencies": {
    "linkedom": "^0.18.5",
    "vitest": "^2.1.8"
  }
}
